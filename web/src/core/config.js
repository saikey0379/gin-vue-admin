/**
 * 网站配置文件
 */

const config = {
  appName: 'IMP',
  appLogo: '/favicon.ico',
  showViteLogo: true
}

export const viteLogo = (env) => {
  if (config.showViteLogo) {
    const chalk = require('chalk')
  }
}

export default config
